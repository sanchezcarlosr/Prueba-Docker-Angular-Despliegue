import { VerificarPrecioDirective } from './verificar-precio.directive';

describe('VerificarPrecioDirective', () => {
  it('should create an instance', () => {
    const directive = new VerificarPrecioDirective();
    expect(directive).toBeTruthy();
  });
});
