import { VerificarDniDirective } from './verificar-dni.directive';

describe('VerificarDniDirective', () => {
  it('should create an instance', () => {
    const directive = new VerificarDniDirective();
    expect(directive).toBeTruthy();
  });
});
